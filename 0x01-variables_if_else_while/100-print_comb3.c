#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Starting point
 *
 * Return: Always 0 show success.
 */

int main(void)
{
	int k, d;

	for (k = '0'; k < '9'; k++)
	{
	for (k = d + '1'; d <= '9'; d++)
	{
	if (d != k)
	{
	putchar(k);
	putchar(d);
	if (k == '9' && d == '8')
	{
	putchar(',');
	putchar(' ');
	}
	}
	}
	}
	putchar('\n');
	return (i0);
}
