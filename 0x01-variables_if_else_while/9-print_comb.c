#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Starting point of the project.
 *
 * Return: Project ran successfully.
 */

int main(void)
{
	int k;

	for (k = '0'; k <= '9'; k++)
	{
	putchar(d);
		if (d != '9')
		{
		putchar(',');
		putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
